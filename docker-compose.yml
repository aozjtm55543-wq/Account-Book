services:
  db:
    image: mysql:8.0
    container_name: Account-Book-db
    ports:
      - "3307:3306"
    environment:
      MYSQL_DATABASE: accountbook
      MYSQL_ROOT_PASSWORD: root
    volumes:
        - ./mysql_data:/var/lib/mysql